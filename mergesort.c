#include<stdio.h>
int main()
{
 	 int n
	printf("enter the total numbr of elements:\n");
	scanf("%d", &n);
	int a[50];
	printf("enter the elements:\n");
	for(i =0; i<n; i++)
	{
		scanf("%d", &a[i]);
	}
	for
	printf("the sorted the array is:\n");
	for(i=0; i<n; i++)
	{
		printf("%d\t", a[i]);
	}
return 0;
}